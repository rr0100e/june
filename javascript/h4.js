/*Write a conditional statement that takes into consideration validations
 (i.e case sensitivity). this will be entire up to you as to what the con
ditional statement is on*/

let month ="may"

if(month=== "jan" || month==="feb" || month==="march" || month==="april" || month==="may"){
    console.log(`${month} is a cold month`)
}else if (month === "june" || month ==="july" ||month==="august") {
    console.log(`${month} is a hot month`)
}else{
    console.log("im not sure")
}

/*Write a conditional statement 
that prints whether the phone being used is an Iphone or an android*/
